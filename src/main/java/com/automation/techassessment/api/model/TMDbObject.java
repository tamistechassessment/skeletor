package com.automation.techassessment.api.model;

/**
 * Marker Interface - Since we are using an external API, enforce compile time checking.
 * If we were using an internal object, I would have used an annotation.
 *
 *  This interface has only been added to Top level API objects.
 */
public interface TMDbObject {
}
